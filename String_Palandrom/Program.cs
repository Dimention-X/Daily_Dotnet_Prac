using System;


namespace String_Palandrom
{
    class Program
    {
        static void Main(string[] args)
        {
            //string len = Console.ReadLine();
            //string sublen = len.Substring(len.Length / 2);
            //string sublen1 = len.Substring(0, len.Length / 2);
            //if (sublen == sublen1)
            //    Console.WriteLine("Strings are palendrom");
            //else
            //    Console.WriteLine("not");
            //Console.ReadLine();

            string len = "Akhilesh";
           
            if(len== )

        }
    }
}
